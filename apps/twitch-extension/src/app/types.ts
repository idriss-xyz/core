export type FormValues = {
  donationLink: string;
};

export type ConfigValues = {
  donationLink: string;
  address: string;
};

export type WidgetVariants = 'panel' | 'videoOverlay' | 'videoComponent';

export type ConfigSubmitStatus = 'error' | 'success';
